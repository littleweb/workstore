# 创作流程调整验证

此处为隔离浏览器测试截图。主题和资产名称使用测试数据，AI 图片响应替换为现有插画，仅用于验证状态更新、布局和导出；不代表真实主题生成质量。

- `comic-v3-story-start.png`：制作故事第一页与右下任务栏。
- `comic-v3-asset-preview.png`：资产生成后放大预览。
- `comic-v3-editor.png`：自动切换到编辑器，对白显示在画内。
- `comic-v3-featured.png`：封面完整适配，裁切视窗不泄漏相邻格子。
- `comic-v3-export.png`：实际导出的 PNG，包含漫画气泡。

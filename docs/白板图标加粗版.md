# 白板图标加粗版

输出：`public/icons/whiteboard-v4.png`。保留 v3 原文件。

使用内置 imagegen 编辑模式，以 v3 透明图标为参考；接口不提供模型版本选择或确认。已检查输出为 RGBA，外部背景透明。

## 最终提示词

Edit this icon: make all teal strokes much thicker and bolder for legibility at 20 pixels. Retain the whiteboard and pencil shapes. Preserve the original image's transparent background, output RGBA PNG with transparent alpha outside the icon. Simple flat teal strokes, white board interior, no surrounding tile.

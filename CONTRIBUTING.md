# 参与 WorkStore

请先阅读 README、AGENTS.md 和 docs/context/CURRENT.md。小修复可以直接提交 PR；较大的功能或数据格式变更先用 Issue 说明目标、兼容性和验证方案。

每个 PR 应说明具体问题、修改后的行为、验证结果和已知限制。涉及用户文件或同步时，增加能覆盖故障场景的测试；不使用真实用户数据作为 fixture。跨设备开发按 docs/DEVELOPMENT.md 操作，并随代码更新共享上下文。

依赖更新需同时提交锁文件。新增素材需说明来源与再分发权限，第三方许可证不可被项目许可证替代。不要将 Token、签名私钥、工作目录或私人对话提交到仓库。

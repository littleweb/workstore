# WorkStore 开发约定

开始工作时读取 `docs/context/CURRENT.md`、`docs/ARCHITECTURE.md`、`docs/DEVELOPMENT.md`。这些文件是跨设备共享的项目上下文；以用户当前要求和实际代码为准，不把历史方案当作已实现功能。

## 必须保持

- 应用是本地优先的容器。用户内容以文件保存；GitHub 是可选传输层，没有必需的自建服务器。
- 子工具通过统一 AI 接口使用模型；默认本地 Codex，但不要描述成离线模型。
- 最近列表已有条目切换不重排；新加入或删除后重新打开的条目排最前。
- 文档、白板的创建按钮位于各自左侧导航底部，不放到主导航。
- 修改列表点击、编辑器焦点或输入法时，先读 `docs/lessons/document-first-click.md`；保留文档列表“仅按下、无后续 click”的选择、尾随 click 去重和输入法保护，不将按下执行盲目推广到所有按钮。
- 网络阶段不得持有工作空间写锁或阻塞用户输入。保存、同步激活和刷新必须保护正在编辑的数据。
- 不删除用户数据来解决同步问题。锁住的 Git 缓存可切换到独立新缓存；保留原缓存和共同基线。
- 同步不再生成工具列表中的“冲突副本”。可合并字段自动合并；重叠版本完整存入 Git 跟踪的 `.workstore-history/sync/`，保留可恢复历史。整理旧副本必须先保全原字节、持有工作区锁并可中断恢复；不能只凭同名判定重复。
- 密钥、Token、设备配置和用户内容不进入本仓库，不输出到日志或提交说明。

## 完成交付

- 每次修改后重新构建桌面包。仅当前平台构建成功不代表所有平台已经验证。
- 运行与修改相关的测试。完整 Rust 回归使用 `--test-threads=1`，避免进程继承文件锁造成并发测试干扰。忽略的联网/模型测试需按任务显式执行，不默认消耗服务额度。
- 更新 `docs/context/CURRENT.md`，写清改动、验证结果、未完成事项和下一步；长期决定更新 `docs/ARCHITECTURE.md`。
- 沟通以简洁中文为主，说明实际结果和限制，不声称尚未验证的平台或远端状态已成功。
- 只有用户要求发布时才发布安装包；不要改写已发布标签。普通开发无须更新签名私钥。
- 多设备分别使用分支或独立 checkout。推送前 fetch，拒绝 force push；冲突应保留各方工作并解决，不覆盖未提交改动。
